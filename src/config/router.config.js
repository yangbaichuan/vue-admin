/**
 * 路由配置
 * @description 当前文件配置系统路由文件。path、name、component均为vue-router配置，meta支持的配置如下
 *  title         页面标题，在左侧菜单、面包屑和标签导航处显示
 *  alias         页面别名，在左侧菜单折叠起来时显示的名称
 *  icon          路由图标，在左侧菜单、面包屑和标签导航处显示的图标
 *  access        权限数组，可访问该页面的权限数组，当前路由设置的权限会影响子路由
 *  href          要跳转的第三方地址，会打开新的标签页
 *  showAlways    一直显示（仅有一个子集的时候也显示父级）
 *  hideInBread   在面包屑中隐藏
 *  hideInMenu    在左侧菜单中隐藏
 */

export default [

];
